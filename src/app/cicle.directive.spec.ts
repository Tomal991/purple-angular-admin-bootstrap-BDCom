import { CicleDirective } from './circle.directive';

describe('CicleDirective', () => {
  it('should create an instance', () => {
    const directive = new CicleDirective();
    expect(directive).toBeTruthy();
  });
});
